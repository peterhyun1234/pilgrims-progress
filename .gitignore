# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node (PPT generation scripts)
node_modules/

# Playwright MCP logs
.playwright-mcp/

# Build artifacts
*.pptx.tmp

# Test screenshots
screenshots/

# QA / dev screenshots that accumulate in repo root
# (asset PNGs under web/public/assets/ are still tracked — this only affects root)
/*.png

# Vite cache
.vite/

# Claude worktrees (임시 작업 브랜치)
.claude/worktrees/
